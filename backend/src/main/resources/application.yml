spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/anook_dev}
    username: ${SPRING_DATASOURCE_USERNAME:anook}
    password: ${SPRING_DATASOURCE_PASSWORD:anook1234}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
