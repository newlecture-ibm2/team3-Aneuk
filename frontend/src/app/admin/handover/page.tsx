import React from 'react';

export default function handoverPage() {
  return (
    <div style={{ padding: 'var(--space-24)' }}>
      <h1 style={{ fontSize: 'var(--text-h2-bold-size)', fontWeight: 'var(--text-h2-bold-weight)', marginBottom: 'var(--space-16)' }}>
        인수인계
      </h1>
      <p style={{ color: 'var(--color-gray-500)' }}>화면 레이아웃 컴포넌트 조립 대기 중입니다.</p>
    </div>
  );
}
